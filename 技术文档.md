# IM-Chat 技术文档（风险核验版）

- 更新时间：2026-02-09
- 代码分支：`dev`（HEAD：`9e5ef9e`）
- 核验范围：`IMChat/`（iOS SwiftUI）+ `backend/`（Go + Gin + GORM）
- 结论摘要：主链路可用；文档中的 7 个风险项里，`配置模板化`与`CORS 白名单`已完成，`可观测性`部分完成，其余仍未完成。

## 1. 当前架构与主链路状态

### 1.1 前端（SwiftUI）

1. 架构分层已落地：`App / Core / Features / Shared`。
2. 登录与会话恢复可用：`AuthViewModel + AppSessionStore`。
3. 聊天链路可用：历史分页 + 乐观发送 + WS 回执替换。
4. 头像链路可用：上传文件后更新用户头像。

关键文件：
- `IMChat/IMChat/Core/DI/AppContainer.swift`
- `IMChat/IMChat/Core/Networking/APIClient.swift`
- `IMChat/IMChat/Core/Realtime/WebSocketService.swift`
- `IMChat/IMChat/Features/Auth/Presentation/AuthViewModel.swift`
- `IMChat/IMChat/Features/Chat/Presentation/ChatViewModel.swift`
- `IMChat/IMChat/Features/Profile/Data/ProfileRepository.swift`

### 1.2 后端（Go）

1. HTTP/WS 鉴权闭环已落地。
2. 消息分页接口已落地。
3. WS 回执含 `clientMessageId + messageId`，支持前端确认乐观消息。
4. 统一响应结构已在多数接口使用。

关键文件：
- `backend/internal/middleware/auth.go`
- `backend/internal/router/router.go`
- `backend/internal/handlers/message.go`
- `backend/internal/ws/hub.go`
- `backend/pkg/response/response.go`

### 1.3 当前工作区状态

1. 本轮已完成后端配置与跨域治理改动：
- 新增 `backend/.env.example`
- 新增 `backend/config.example.yaml`
- 更新 `backend/internal/router/router.go`（CORS 白名单）

## 2. 问题与风险核验结果（逐项）

核验时间：2026-02-09。

| 编号 | 风险项 | 状态 | 核验证据 | 结论 |
| --- | --- | --- | --- | --- |
| 1 | 配置治理未闭环 | 已完成（本轮） | 已新增 `backend/.env.example` 与 `backend/config.example.yaml`；`.gitignore`已忽略本地敏感配置文件；`config.go`支持环境变量覆盖 | 模板化闭环已完成 |
| 2 | CORS 配置冲突 | 已完成（本轮） | `backend/internal/router/router.go`已改为从 `CORS_ALLOW_ORIGINS` 读取白名单并按请求 `Origin` 回写，不再使用 `*` | 冲突已消除 |
| 3 | 响应风格不统一 | 未完成 | `backend/internal/handlers/upload.go`仍返回`c.JSON(http.StatusOK, gin.H{"url": url})`；前端`APIClient`仍保留`wrapped/raw`双风格解析 | 需统一 |
| 4 | 分层边界未收敛 | 未完成 | `backend/internal/handlers/user.go`、`backend/internal/handlers/user_list.go`、`backend/internal/ws/client.go`仍直接使用`models.DB`或`models.*` | 需继续收敛到`handler -> service -> model` |
| 5 | 前端环境配置硬编码 | 未完成 | `IMChat/IMChat/App/Config/Constants.swift`仍硬编码`serverIP` | 需改为环境化配置 |
| 6 | 自动化测试缺失 | 未完成 | 未检索到后端`*_test.go`，也未检索到前端测试文件 | 需补基础测试 |
| 7 | 可观测性不足 | 部分完成 | 使用`gin.Default()`有基础访问日志，WS有`log.Printf`错误日志；未见`request_id/user_id/latency`等结构化字段与指标 | 仍不足以支撑上线定位 |

## 3. 已完成 / 未完成清单（按你文档问题项）

### 3.1 已完成

1. 配置模板化已完成：
- `backend/.env.example`
- `backend/config.example.yaml`
2. CORS 白名单改造已完成：
- `backend/internal/router/router.go` 改为 allowlist 策略
- 不再使用 `Access-Control-Allow-Origin: *` 与 `Allow-Credentials: true` 组合

### 3.2 部分完成

1. 可观测性
- 已做：有基础 access log 与错误日志。
- 未做：无统一 trace/request_id、user_id、latency 指标和结构化日志规范。

### 3.3 未完成

1. 上传接口响应与全局响应风格不一致。
2. 分层边界未收敛（handler/WS 直连 model/DB）。
3. 前端服务地址硬编码。
4. 前后端自动化测试缺失。

## 4. 文档更新点（相对上一版）

1. 将风险条目从“描述型”更新为“核验型”，每项增加状态与证据。
2. 同步“配置模板化”和“CORS 白名单”两项为已完成状态。
3. 将分支快照同步为当前 HEAD：`9e5ef9e`。

## 5. 下一步建议（按优先级）

### P1

1. 统一 `/api/upload` 响应为 `code/msg/data`。
2. 抽离 `user handler` 与 `ws client` 里的 DB/model 直连逻辑到 service。
3. 将前端 `Constants.serverIP` 改为可切换配置源（如 xcconfig 或环境注入）。

### P2

1. 先补 3 个后端测试：鉴权中间件、消息分页、上传 handler。
2. 先补 2 个前端测试：`AuthViewModel`、`ChatViewModel`。
3. 增加请求链路字段：`request_id`、`user_id`、`latency_ms`。

## 6. 总体判断

1. 以课程/演示为目标：当前版本可继续推进。
2. 以上线为目标：需先完成 P1 并补齐 P2 关键项，再进入灰度评估。
